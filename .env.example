# Database Configuration
# Required: PostgreSQL database URL for data storage
DATABASE_URL=postgresql://username:password@localhost:5432/autonova_db

# Environment Configuration
# Set to 'production' for deployment, 'development' for local development
NODE_ENV=production

# Replit Configuration (when deploying on Replit)
# Automatically set by Replit, but can be manually configured if needed
REPL_ID=your-repl-id