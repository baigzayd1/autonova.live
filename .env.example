# Environment Variables Template for Autonova.live
# Copy this file to .env and fill in the actual values

# === DATABASE CONFIGURATION ===
# PostgreSQL connection string
# For Neon Database: postgresql://username:password@hostname/database?sslmode=require
# For local PostgreSQL: postgresql://username:password@localhost:5432/database_name
DATABASE_URL=postgresql://your_username:your_password@your_host:5432/your_database

# === APPLICATION CONFIGURATION ===
# Environment mode (development | production)
NODE_ENV=development

# Server port (default: 5000 for Replit)
PORT=5000

# === REPLIT SPECIFIC (Auto-populated by Replit) ===
# These are automatically set by Replit when deployed
REPL_ID=
REPL_SLUG=
REPL_OWNER=

# === SESSION CONFIGURATION ===
# Secret key for session management (generate a secure random string)
SESSION_SECRET=your_super_secure_session_secret_here

# === OPTIONAL: EXTERNAL SERVICES ===
# Add any external API keys or service configurations here
# EXAMPLE_API_KEY=your_api_key_here
# SMTP_HOST=your_smtp_host
# SMTP_PORT=587
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password

# === DEPLOYMENT NOTES ===
# 1. Never commit this file with actual values to version control
# 2. In Replit, set these variables in the "Secrets" tab
# 3. For DATABASE_URL, use a service like Neon, Supabase, or PlanetScale
# 4. Generate a strong SESSION_SECRET using a random string generator